package com.example.cardservice.service;

import com.example.cardservice.model.Account;
import com.example.cardservice.model.Card;
import com.example.cardservice.model.CardRequest;
import com.example.cardservice.model.Card;
public class MariaDBService {
    public void saveCard(Card card) {
        // implementation to save card to database
    }

    public void updateCard(Card card) {
        // implementation to update card in database
    }

    public void saveAccount(Account account) {
        // Implementation to save an account in the database
    }

    public void saveCardOrder(CardRequest cardRequest) {
        // Implementation to save a card order in the database
    }
}
